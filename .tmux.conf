# Source session-specific config file if it exists
# Checks for /tmp/tmux.[session_name] and sources it
set-hook -g session-created 'run-shell -b "[ -f /tmp/tmux.#{session_name} ] && tmux source-file /tmp/tmux.#{session_name}"'
